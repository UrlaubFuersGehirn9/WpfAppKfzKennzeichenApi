<Window x:Class="WpfAppKfzKennzeichenApi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppKfzKennzeichenApi"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <Button x:Name="btnGetKennzeichen" Grid.Column="0" Grid.Row="1" Margin="5" Click="btnGetKennzeichen_Click">Get KennzeichenInfos</Button>
        <Button x:Name="btnBeenden" Grid.Column="1" Grid.Row="1" Margin="5" Click="btnBeenden_Click">Beenden</Button>
        <DataGrid x:Name="gridKennzeichen" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"></DataGrid>
        <ListBox x:Name="lbKennzeichen" Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2"></ListBox>
    </Grid>
</Window>
